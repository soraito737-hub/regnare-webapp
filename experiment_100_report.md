# 5カテゴリ判定モジュール 100件分類実験レポート

## 目的
`modulus/embedding_classifier.py`（Gemini Embeddingによる5カテゴリ分類）の実際の分類性能を、新規に作成した100件のテストコメントで検証する。

## 準備したもの

### 1. テストデータセット（新規100件）
[data/test_dataset_100.json](data/test_dataset_100.json) を作成。既存の教師データ（`data/training_examples.json`）・既存テストセット（`data/test_dataset.json`, 39件）とは重複しない新規コメント。

| 区分 | 件数 | 内訳 |
|---|---|---|
| 外見 | 10 | 該当6 / グレー4 |
| 人間性 | 10 | 該当6 / グレー4 |
| 活動クオリティ | 10 | 該当6 / グレー4 |
| モラル・マナー説教 | 10 | 該当6 / グレー4 |
| 嫉妬型 | 10 | 該当6 / グレー4 |
| 非該当（安全なコメント） | 50 | 称賛・質問・雑談など |
| **合計** | **100** | |

### 2. 実行環境の構築
このPCにはPythonが実質未インストール（`python`/`py`はMicrosoft Storeのスタブのみ）だったため、以下を実施。
- `winget install Python.Python.3.12` で Python 3.12.10 を導入
- `ensurepip` で pip をブートストラップ
- `pip install google-genai scikit-learn numpy` で依存パッケージを導入
- ユーザー提供の `GEMINI_API_KEY` を使い、疎通確認後に本実験を実行

### 3. 実行スクリプト
- [modulus/run_100_experiment.py](modulus/run_100_experiment.py)（新規）: 教師データ50件をベクトル化 → 100件を`CommentClassifier.classify()`で分類 → 結果を保存
- [modulus/corrected_analysis.py](modulus/corrected_analysis.py)（新規）: 実験結果のキャッシュ済み類似度を使い、評価ロジックのバグを修正した上で再集計（追加API呼び出しなし）

生データは [data/experiment_100_results.json](data/experiment_100_results.json)（100件全件の予測内訳）、[data/corrected_analysis.json](data/corrected_analysis.json)（修正後の集計）に保存済み。

---

## 結果

### ⚠️ 前提: 評価スクリプトにバグを発見
`modulus/evaluate_thresholds.py`の`evaluate()`/`apply_threshold()`は、類似度スコアのみで判定を再計算しており、本番の`embedding_classifier.classify()`が持つ「最も近いカテゴリが『非該当』なら無条件で非該当とする」という上書きルールを無視している。

その結果、**素の`evaluate_thresholds.py`のロジックで100件を評価すると Precision=0.5, Accuracy=0.5 という異常値**が出た（安全なコメントの多くが「非該当」カテゴリの教師データと高い類似度＝0.6以上を示すため、閾値だけ見ると誤って「該当」判定に見えてしまう）。

これは**評価スクリプト側の不具合**であり、実際の本番ロジック（`classify()`が返す`judgement`）とは一致しない。下記の結果は、本番ロジックと同一の判定基準で再集計した正しい数値。

### 現行閾値（THRESHOLD_MATCH=0.60, THRESHOLD_GRAY=0.45）での性能

| 指標 | 値 |
|---|---|
| Precision（適合率） | **94.3%** |
| Recall（再現率） | **100%** |
| F1 | **97.1%** |
| Accuracy（正解率） | **97%** |
| カテゴリ一致率 | 88%（44/50） |
| グレー判定になった件数 | **0件** |

- True Positive: 50/50（該当・グレーの50件は全て「安全ではない」と正しく検知）
- False Positive: 3/50（安全な50件のうち3件を誤検知）
- False Negative: 0

### グリッドサーチ（閾値0.60〜0.95の範囲）
閾値を0.60〜0.95の間でどう動かしても、Precision/Recall/F1は変化しなかった（上位10パターンすべて同一スコア）。理由は後述。

---

## 発見した問題点

### 1. 「グレー」判定帯が実質機能していない
今回のテストでは、閾値を0.60〜0.95のどこに設定しても**「グレー」と判定されたコメントは0件**だった。理由は、カテゴリに関連するコメント（「該当」「グレー」どちらの正解ラベルでも）の類似度が軒並み0.79〜0.97と高く、逆に無関係なコメントは「非該当」カテゴリとの一致で早期に弾かれるため、`THRESHOLD_MATCH`と`THRESHOLD_GRAY`の間の"境界"にほとんど何も落ちてこない。

→ 現状の埋め込み類似度は「話題が関連するか」は精度良く捉えられるが、「該当（明確に有害）」と「グレー（あいまい・軽微）」という**深刻度の強弱**の区別には効いていない可能性がある。3タブ構成（通常/気になる/確認待ち）の「確認待ち」タブが、本番でも同様にほぼ使われない設計になっているリスクがある。

### 2. 誤検知（False Positive）3件の内容
| コメント | 誤って一致したカテゴリ | 類似したと判定された教師データ |
|---|---|---|
| 声のトーンが落ち着いてて聞きやすいです | 嫉妬型 | 楽して動画撮って、リスナーから投げ銭集めていいご身分ですね。 |
| 質問なんですが、使ってるカメラは何ですか？ | モラル・マナー説教 | 撮影許可ちゃんと取った？一般人が映り込んでてマナー違反だよ。 |
| 配信環境整ってきましたね、快適に見れます | 嫉妬型 | 楽して動画撮って、リスナーから投げ銭集めていいご身分ですね。 |

いずれも「配信・撮影・お金」といった**表層的な単語の共起**に引っ張られた誤検知で、文脈上の悪意は全くない。

### 3. カテゴリ取り違え（6件、該当/グレー50件中）
| 正解 | 予測 | 件数 |
|---|---|---|
| 人間性 → 外見 | 2件 |
| 活動クオリティ → 人間性 | 1件 |
| 活動クオリティ → モラル・マナー説教 | 1件 |
| モラル・マナー説教 → 嫉妬型 | 1件 |
| 嫉妬型 → モラル・マナー説教 | 1件 |

「有害コメントかどうか」の判定（Accuracy 97%）は高精度だが、**5カテゴリのどれに当たるかの取り違えは一定数ある**（特に人間性⇔外見、モラル・マナー説教⇔嫉妬型の混同）。ユーザーが選んだ「見たくないカテゴリ」で振り分ける今の設計では、カテゴリを外すと「気になる」タブに出るはずのコメントが「通常」タブに漏れる可能性がある。

---

## 生成物一覧
- [data/test_dataset_100.json](data/test_dataset_100.json) — 新規100件テストセット
- [modulus/run_100_experiment.py](modulus/run_100_experiment.py) — 実験実行スクリプト
- [modulus/corrected_analysis.py](modulus/corrected_analysis.py) — 修正版の再集計スクリプト（API呼び出し不要）
- [data/experiment_100_results.json](data/experiment_100_results.json) — 100件全件の生データ（類似度・予測・正解ラベル）
- [data/corrected_analysis.json](data/corrected_analysis.json) — 修正後の集計結果

## 推奨アクション
1. `evaluate_thresholds.py`の`apply_threshold()`を修正し、`classify()`と同じ「非該当カテゴリへの一致は無条件で非該当」ルールを反映する（現状は評価結果が信用できない）。
2. 「グレー」帯の設計意図（軽微・あいまいなコメントを人間の確認に回す）が機能するよう、閾値方式以外の見直しを検討する（例: カテゴリ内で複数の重み付き教師データを使う、深刻度を別軸でスコアリングする等）。
3. カテゴリ混同（人間性⇔外見、モラル・マナー説教⇔嫉妬型）に対応する教師データを増やし、境界事例の分離を改善する。
